# Application
APP_NAME="Portfolio"
APP_ENV=production
APP_KEY=  # Generate with: php artisan key:generate
APP_DEBUG=false
APP_URL=https://portfolio-production-bff4.up.railway.app

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

# Logging
LOG_CHANNEL=stack
LOG_LEVEL=debug

# Database (Railway)
DB_CONNECTION=mysql
DB_HOST=hopper.proxy.rlwy.net
DB_PORT=30702
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=your_actual_database_password_here

# Sessions / Cache / Queue
SESSION_DRIVER=file
QUEUE_CONNECTION=sync
CACHE_STORE=file

# Mail (Brevo SMTP)
MAIL_MAILER=smtp
MAIL_HOST=smtp-relay.brevo.com
MAIL_PORT=587
MAIL_USERNAME=8c442a001@smtp-brevo.com
MAIL_PASSWORD=your_actual_mail_password_here
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=sebastian@pumo.digital
MAIL_FROM_NAME="${APP_NAME}"

# Vite (Frontend assets)
VITE_APP_NAME="${APP_NAME}"
